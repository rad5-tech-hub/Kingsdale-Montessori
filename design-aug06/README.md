Files extracted from commit `f6805fb65ea63b08e1d5b42e1bc2b24448cd018f` (2026-08-06 08:43:57 +0100).

Contents:
- `index.aug06.html` — HTML from the Aug 6 commit
- `styles.aug06.css` — CSS from the Aug 6 commit (was `css/styles.css`)
- `main.aug06.js` — JS from the Aug 6 commit (was `js/main.js`)

Purpose: This folder contains the Aug 6 design snapshot so you can edit it separately as the main design to be merged back later. Do not commit these original snapshot files over the working files unless intended.

Working copies created (uncommitted):
- `index.aug06.modified.html` — Aug 6 HTML with merged updates (see tasks applied)
- `styles.aug06.modified.css` — CSS with overrides and responsive fixes

Notes:
- Originals remain as `index.aug06.html` / `styles.aug06.css` / `main.aug06.js`.
- The modified files are intentionally left uncommitted in this folder; commit them only when ready to merge.
